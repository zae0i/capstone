package app.greenpoint.repository;

import app.greenpoint.domain.EsgRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EsgRuleRepository extends JpaRepository<EsgRule, Long> {
}
